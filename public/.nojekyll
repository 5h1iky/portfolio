此文件的作用：告诉 GitHub Pages 不要用 Jekyll 处理这个站点。

GitHub Pages 默认会用 Jekyll 渲染，而 Jekyll 会忽略以下划线开头的文件/目录。
Vite 生成的资源目录恰好叫 assets（不带下划线，本来没事），
但保留这个空文件是最省事的保险做法——万一以后构建产物里出现下划线开头的
文件名，也不会被 Jekyll 悄悄吞掉。

内容为空是正常的，不要删这个文件。
