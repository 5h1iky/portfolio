# 新会话交接指引

> 面向对象：接手这个项目的 AI 助手
> 最后更新：2026-09-12

---

## 你现在该做什么

制作者会在**新工作区**打开 `D:\www\portfolio`，然后说类似"继续做作品集网站"的话。

**按这个顺序读文件，读完了再动手：**

1. `README.md` —— 项目是什么、当前状态
2. `docs/REQUIREMENTS.md` —— **需求，必读**。特别是第 2.3 节（第一优先级）和第 2.4 节（硬件约束）
3. `docs/TECH_PLAN.md` —— 技术选型、环境现状、**第 3 节的 C 盘防护措施必须照做**
4. 本文件 —— 了解历史背景

---

## 项目现状

**代码一行都还没写。** 这个仓库目前只有四份 Markdown 文档。
但**需求已经完全定完了**——需求第 3 节的 10 个待定问题已由制作者全部拍板
（风格 / 配色 / 称呼 / 联系方式 / 头像 / 语言 / 深色模式 / 博客 / 部署 / 域名）。

所以你不是"接手一个进行中的项目"，也不是"从需求归档开始"，
而是**"需求已冻结，直接动手写第一版"**。不要再问一遍那 10 个问题。

---

## 必须知道的几条硬约束

整理自需求和本次环境核查，**违反其中任何一条都会给制作者带来实际麻烦**：

| # | 约束 | 原因 |
|---|---|---|
| 1 | **项目固定在 `D:\www\portfolio`**，不要移到桌面或 C 盘 | C 盘只剩 13.4 GB |
| 2 | **启动开发服务器用 `.\dev.ps1`**，不要直接 `npm run dev` | 脚本会把 TEMP 重定向到 D 盘，否则构建会写 C 盘 |
| 3 | **不要用 `npm install -g`**，工具一律 `npx` 本地调用 | 全局包目录还在 C 盘 |
| 4 | **不要动系统环境变量** | 制作者要求重定向只在项目内做 |
| 5 | **不要动 `C:\Users\admin\Desktop\www.cetools.com`** | 那是 SAChat 安卓源码，不是本项目 |
| 6 | **本地开发端口用 5173**，不要用 3080 | 3080 被 DeepSeek Harness 占用 |
| 7 | **不要编造内容** | 制作者还没想好网站放什么，见需求第 2.2 节 |
| 8 | **不要替制作者登录 GitHub 或绕过任何账号验证** | 已明确拒绝过，见下方背景 |

---

## 关于 GitHub 账号（背景，避免重复踩坑）

制作者的 GitHub 账号（`5h1iky`）**当前登录不上**：
2FA 密钥丢失，且这台电脑上**确实没有备份**。

已经做过的排查（**结论：本机无密钥、无恢复码，不要再重复查一遍**）：
- Edge 全部历史、下载、自动填充、保存的账号
- `basetoolbox.com` 自己的 IndexedDB（当时用的网页 TOTP 生成器，纯内存计算，不留记录）
- 浏览器 LocalStorage / SessionStorage / Cache
- Windows 凭据管理器（只有 git 凭据 `git:https://github.com`，用户 `5h1iky`）
- C/D/E/F 四盘的文件名与文本内容检索
- 回收站（解析了 `$I` 元数据，含已删文件原名）
- 19,500 张图片全量 OCR
- 剪贴板历史 / Windows 时间线

**这阻塞开发吗？不阻塞。** 本地开发完全用不到账号。
只有最后部署那一步可能涉及，方案见 `TECH_PLAN.md` 第 5 节。
如果制作者又提起"能不能直接登录"，参考上面的结论回答，**不要尝试绕过验证**。

---

## 开工建议顺序

1. ~~先确认制作者是否已经想好网站内容~~ —— **已完成**，10 项决定全部拍板，见需求第 3 节
2. 初始化 Vite + React + Tailwind 项目，**先配好 `.npmrc` 和 `dev.ps1` 再装依赖**
3. 建好 `src/data/profile.js`，把 SAChat 填进去（素材从 `www.cetools.com` 复制，不修改原目录）
4. 跑起来给制作者看，让他提意见，再迭代
5. 制作者满意后再讨论部署

> **注意**：风格要跟 SAChat 的 Material 风格，但**配色用紫蓝渐变**
> （制作者知情后选的，不是青色）。别看到 SAChat 的色板就自作主张改回去，理由见需求 3.1。

---

## 一个提醒

制作者的第一优先级是**"方便我后期改动"**，不是"功能多"也不是"技术先进"。
每做一个技术决定，都问自己：**这会让制作者以后改内容变得更简单，还是更麻烦？**
如果答案是后者，就换一个做法。
